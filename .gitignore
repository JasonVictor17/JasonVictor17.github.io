# 这份文件由 python_project/web_publish.py 在每次发布前拷到本仓根目录当 .gitignore。
# 改这里 = 改公开仓的入口闸门，必须同步改 web_publish.py 里的 PUBLISH_ALLOWLIST，
# 两边对不上发布会直接失败（check_allowlist）。

# website 只承载 GitHub Pages 静态产物：任何 Python 源码都不许进公开仓库
# （数据生成与回测代码已迁到 python_project/etf、python_project/data_process）
*.py
*.pyc
__pycache__/
/deploy/

# ---- chart 白名单：默认全拒，只有下面显式放行的文件能进公开仓 ----------------
# 用白名单而不是「逐个忽略不要的文件」，是因为后者会漏 —— 回测每加一种输出格式
# （csv / png / parquet …）就会静默上线，而公开仓的历史删不掉。
# /chart/** 连目录一起拒，所以先用 !/chart/**/ 把目录放回来，才能再放行具体文件。
/chart/**
!/chart/**/
!/chart/index.html
!/chart/etf/index.html
!/chart/smallcap/index.html
!/chart/momentum/index.html
!/chart/dividend/index.html
!/chart/backtest/index.html
!/chart/logo.svg
!/chart/data/etf/backtest_etf.json
!/chart/data/etf/backtest_etf_records.csv
!/chart/data/smallcap/backtest_smallcap.json
!/chart/data/momentum/momentum_latest.json
!/chart/data/momentum/backtest_momentum.json
!/chart/data/momentum/backtest_snapshots.json
!/chart/data/dividend_history.json
!/chart/data/dividend_latest.json
!/chart/data/dividend_scatter.json

# 注：.gitignore 对**已跟踪**文件无效。仓里还留着几个页面并不读的存量产物
# （backtest_stock_records.csv、各 *_plot.png、dividend_scatter.jpg、
#   backtest_rolling.json、momentum_history.json），要停发得 git rm --cached。
# ---- 白名单结束 --------------------------------------------------------------
